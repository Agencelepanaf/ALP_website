/*
 * Les polices sont chargées via CSS (globals.css) pour éviter les erreurs de build
 * dans les environnements sans accès réseau à Google Fonts.
 *
 * Pour passer à next/font/google (recommandé en production Vercel) :
 *   import { Inter, Instrument_Serif } from "next/font/google"
 *   Supprimer l'@import dans globals.css
 *   Utiliser inter.variable et instrumentSerif.variable dans layout.tsx
 */

export const fontVariables = "";
