/*
 * MarqueeLogos — défilement horizontal des logos clients.
 * À activer quand les premiers clients seront référencés.
 * Remplacer le placeholder ci-dessous par les logos réels.
 */

// "use client";
// import { motion } from "framer-motion";

export default function MarqueeLogos() {
  return (
    <div className="py-8 border-t border-b border-gray-border text-center text-gray-muted text-sm tracking-widest uppercase">
      Vos logos clients ici bientôt
    </div>
  );
}
